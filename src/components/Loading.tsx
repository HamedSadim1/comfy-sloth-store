import React from "react";

// Functional component for loading spinner
const Loading: React.FC = () => {
  return (
    <div className="section section-center">
      <div className="loading"></div>
    </div>
  );
};

export default Loading;
