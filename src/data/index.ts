// Legacy `colors` palette export was removed: dummyjson exposes no
// colour per product, so the brand-filter swatch picker that
// imported this hardcoded 5-tone list was deleted in lockstep.
// This file stays as a barrel re-export for any future data
// modules and intentionally exports nothing today.
